name: Release
on: 
  workflow_dispatch: 
    pull_request:
      types: [opened, synchronize, reopened]
    push:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - run: echo "✅ GitHub Actions is working"

